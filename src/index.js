import React from 'react';
import ReactDOM from 'react-dom';
import RoutePages from './catalogue-products/routePages.js';

ReactDOM.render(
  <React.StrictMode>
    <RoutePages />
  </React.StrictMode>,
  document.getElementById('root')
);